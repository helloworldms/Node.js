mixin videoBlock(video = {})
    videoBlock
        a(href=routes.videoDetail(video.id))
        video.videoBlock__thumbnail(src=video.videoFile, autoplay=true)
        h4.videoBlcok__title=video.title
        h4.videoBlcok__title=video.viewsc